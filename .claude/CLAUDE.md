# Skills

- **write-stories** — when creating or updating Storybook stories for components (`src/components/**/*.stories.ts`).
- **styles** — when styling components or editing theme tokens (semantic alias CSS variables only; never primitives).
